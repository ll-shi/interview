/**
 * 数组的常用方法
 * 1）push
 * 2）pop
 * 3）shift 跟pop相反，弹出index=0的值，并且改变数组长度跟值。
 * 4) unshift 跟push相反，添加一个值到index = 0 的位置
 * 5) reverse 数组反转
 * 6）flat 数组扁平化
 * 7）splice 删除数组的某几项，影响愿数组。
 * 8) slice  跟splice功能相同，不影响原数组。
 * 9）sort 排序，需要传入一个compare的回调
 * 10）concat 拼接2个数组
 * 11）join 把数组转换成字符串，一般配合string的split使用（常用）
 */
/**
 * 伪数组 
 * 1）arguments 就是一个伪数组
 * 特点
 * 1）属性为数字属性
 * 2）有length属性
 */